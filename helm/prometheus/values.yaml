replicaCount: 1
fullnameOverride: prometheus

image:
  repository: prom/prometheus
  tag: v3.6.0

service:
  type: ClusterIP
  port: 9090

scrapeInterval: 15s
retention: 7d
collectorTarget: otel-collector:8889

resources:
  requests:
    cpu: 100m
    memory: 512Mi
